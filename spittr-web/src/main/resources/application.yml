server:
  port: 80

logging:
  level:
    spittr: DEBUG
spring:
  application.name: Bootstrap Spring Boot
  thymeleaf:
    cache: false
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
